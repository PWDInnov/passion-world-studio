rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Public can read any data
    // Only authenticated users can write (add, update, delete)
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    
  }
}